# Product Requirements

Build a local AI operations dashboard with pack management, workflow execution, trace logging, approvals, and evaluation reports. The MVP must run with Docker Compose and avoid destructive tools by default.
