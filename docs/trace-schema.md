# Trace Schema

Trace spans store run_id, span_id, parent, timestamp, agent, node, event type, input/output summaries, tool input/output, policy decision, risk level, latency, token usage, cost, status, and error message.

## Extension notes

Keep README claims tied to real code. Add tests whenever changing this module.
