# Docker Deployment

Docker Compose starts PostgreSQL, Redis, FastAPI, Next.js, a worker placeholder, and optional Ollama. Use `.env.example` as the starting point.

## Extension notes

Keep README claims tied to real code. Add tests whenever changing this module.
