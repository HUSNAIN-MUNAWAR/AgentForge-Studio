# Limitations

Known limitations: no production auth, no multi-tenant isolation, no drag-and-drop graph editing, background execution is FastAPI-local, and web search is disabled unless configured.

## Extension notes

Keep README claims tied to real code. Add tests whenever changing this module.
