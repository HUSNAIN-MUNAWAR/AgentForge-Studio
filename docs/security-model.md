# Security Model

The local security model blocks destructive tools, keeps provider keys on the backend, sanitizes uploads, limits file size, requires approval for risky tools, and logs all tool calls.

## Extension notes

Keep README claims tied to real code. Add tests whenever changing this module.
