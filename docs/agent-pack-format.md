# Agent Pack Format

Agent Packs are YAML files containing metadata, models, agents, workflow, tools, memory, policies, and evaluation settings. Validation is implemented in `agent_engine/pack_schema.py` using Pydantic.

## Extension notes

Keep README claims tied to real code. Add tests whenever changing this module.
