# Workflow Engine

The workflow engine is a lightweight graph runner. It orders nodes from workflow edges, runs local tools, calls an LLM provider adapter, writes trace spans, and stops when approval is required.

## Extension notes

Keep README claims tied to real code. Add tests whenever changing this module.
