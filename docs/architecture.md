# Architecture

Architecture explains how Next.js, FastAPI, PostgreSQL, Redis, the agent engine, policy engine, tool runtime, and trace recorder work together. The backend is the trusted boundary for API keys and tool execution. The frontend consumes REST and WebSocket events.

## Extension notes

Keep README claims tied to real code. Add tests whenever changing this module.
