# Policy Engine

The policy engine returns allow, block, or require_human_approval. It checks pack blocked tools, pack approval tools, explicit rules, and risk defaults.

## Extension notes

Keep README claims tied to real code. Add tests whenever changing this module.
